name: Documentation Validation
on: [pull_request]
jobs:
  validate-docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate documentation state
        env:
          DOCKIT_SKIP_EXTERNAL: "1"
        run: scripts/dockit-validate-session.sh --human
