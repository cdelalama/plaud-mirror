// Vitest setup file. Loads @testing-library/jest-dom matchers
// (toBeInTheDocument, toHaveClass, toHaveTextContent, etc.) so component tests
// can assert against rendered DOM with a familiar vocabulary.
//
// Configured by vitest.config.ts → test.setupFiles.
import "@testing-library/jest-dom/vitest";
